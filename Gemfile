# frozen_string_literal: true

ruby '2.6.0'
source 'https://rubygems.org'

# Commit contains fix for discordrb#684 in lieu of discordrb team ever doing a patch release
gem 'discordrb', git: 'https://github.com/discordrb/discordrb.git', ref: 'bb457fe'
gem 'dotenv'
gem 'flickr'
gem 'httparty'

group :test do
  gem 'rspec'
  gem 'simplecov'
end
